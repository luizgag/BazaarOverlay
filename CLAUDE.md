FOLLOW TDD: WRITE FAILING TESTS BEFORE WRITING IMPLEMENTATION
DON'T CREATE BRANCHES - WRITE EVERYTHING DIRECTLY ON MAIN, THEN COMMIT AND PUSH ONCE WORK IS FINISHED
When reading files, use Grep/Glob to locate relevant sections first, then use offset and limit parameters to read only
the needed lines instead of entire files.
